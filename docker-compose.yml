services:
  bot:
    build: .
    container_name: emoratio-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - bot_data:/app/data
      - model_cache:/app/.cache

volumes:
  bot_data:
  model_cache:
